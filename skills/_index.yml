generated: '2026-07-27'
method: generated
note: >-
  PJM publishes no AGENTS.md, skill files or agent guidance of its own (searched
  2026-07-27). These skills are generated by API Evangelist from PJM's own published
  PDF guides and FAQs plus live anonymous probes. Because no OpenAPI is retrievable,
  every step is grounded in a verbatim documented HTTP call rather than an
  operationId — no operation, parameter or endpoint below is invented.
skills:
- file: pjm-query-data-miner-feed.md
  name: Query a PJM Data Miner 2 feed with paging
  api: PJM Data Miner 2 API
  base_url: https://api.pjm.com/api/v1
  operations:
  - GET /api/v1/{feed}/metadata
  - GET /api/v1/{feed}
  source: PJM Data Miner API Guide v15 (2026-02-10)
- file: pjm-watch-tool-change-notices.md
  name: Watch PJM tool change and outage notices
  api: PJM Messages Public Web Service
  base_url: https://messages.pjm.com/messages/rest/public
  operations:
  - GET /messages/rest/public/messages
  source: https://www.pjm.com/markets-and-operations/etools/upcoming-changes (verified HTTP 200)
  anonymous: true
- file: pjm-authenticate-browserless-etools.md
  name: Authenticate a browserless client to PJM eTools
  api: PJM Browserless Authentication API
  base_url: https://sso.pjm.com/access
  operations:
  - POST /access/authenticate/
  - POST /access/logout/
  - POST /access/authenticate/pjmauthcert
  source: PJM Browserless Authentication Guide and PKI-Based Authentication Guide
not_generated:
  reason: >-
    No skill was authored for the OASIS template surface beyond the authentication
    handshake, because the NAESB template names, required fields and response shapes
    are documented only inside the OASIS API User Guide PDF's template catalog and
    could not be transcribed with confidence at this pass. PJM's own Java CLI is the
    documented path — see cli/pjm-cli.yml.
